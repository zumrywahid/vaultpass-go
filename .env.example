# VaultPass Go Backend — Environment Configuration

# Server
PORT=8080
ENV=development

# MySQL
DATABASE_DSN=root:yourpassword@tcp(127.0.0.1:3306)/vaultpass?parseTime=true

# JWT (MUST change in production)
JWT_SECRET=dev-secret-change-in-production
